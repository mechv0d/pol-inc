services:
  - type: web
    name: pol-inc-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn pol_inc.bot.webhook:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz